{
  "name": "google-remember-sso",
  "version": "0.1.0",
  "description": "Enables choosing a default account for a given Google Account chooser",
  "scripts": {
    "dev": "webextension-toolbox dev",
    "build": "webextension-toolbox build",
    "lint": "tslint --project ."
  },
  "devDependencies": {
    "ts-loader": "^5.3.2",
    "tslint": "^5.12.0",
    "typescript": "^3.2.2",
    "webextension-toolbox": "latest"
  }
}
